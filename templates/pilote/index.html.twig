{% extends 'base.html.twig' %}

{% block title %}Hello PiloteController!{% endblock %}

{% block body %}

    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            {% for pilote in pilotes %}
            <tr>
                <td>{{ pilote.id }}</td>
                <td>{{ pilote.nom }}</td>
                <td>{{ pilote.prenom }}</td>
                <td>{{ pilote.email }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
